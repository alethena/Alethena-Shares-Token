pragma solidity ^0.4.24;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/InstructionToken.sol";

contract TestInstructionToken {


// THIS WILL CONTAIN AUTOMATED UNIT TESTS



   /** function testInitialBalanceUsingDeployedContract() {
        IT = InstructionToken(DeployedAddresses.InstructionToken());
        uint expected = 0;
        Assert.equal(IT.balanceOf(tx.origin), expected, "Owner should have 0 Coins initially");  
    }
    **/

}